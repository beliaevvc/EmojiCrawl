/**
 * Совместимый proxy‑реэкспорт `GameScreen`.
 *
 * Слой: UI (Framework).
 *
 * ## Что это
 * Исторический “вход” для импорта `GameScreen`, который оставлен ради обратной совместимости.
 *
 * ## Зачем
 * В Блоке 3 мы перенесли реализацию `GameScreen` в новую структуру:
 * `src/components/game/screens/GameScreen.tsx`.
 *
 * Но чтобы не ломать старые импорты и не делать массовую замену по проекту,
 * этот файл остаётся как стабильная точка импорта.
 *
 * ## Что делает
 * Просто реэкспортирует `default` из нового пути.
 *
 * ## Инварианты / правила
 * - Этот файл не должен содержать логики и зависимостей.
 * - Новому коду предпочтительнее импортировать `GameScreen` из `src/components/game/screens/GameScreen.tsx`
 *   (через алиас `@/components/game/screens/GameScreen`).
 */
export { default } from './game/screens/GameScreen';
