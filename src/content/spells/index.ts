/**
 * Content Layer — spells module (публичный вход).
 *
 * Почему есть отдельный index:
 * - единая точка импорта для модуля заклинаний,
 * - позже можно добавить разные pack’и/оверрайды, не меняя потребителей.
 */
export * from './baseSpells';


