/**
 * Content Layer — публичный вход в статический контент игры.
 *
 * Слой: Content (public API).
 *
 * ## Что это
 * Единая точка входа для импорта из content слоя.
 *
 * На Блоке 4 это становится “единым источником” для сборки GameContent packs.
 *
 * Зачем нужен этот файл:
 * - не расползаются импорты по “глубоким путям” (`src/content/spells/baseSpells` и т.п.),
 * - удобнее контролировать границы: кто и что может импортить из content слоя,
 * - упрощает дальнейшее переименование/перестройку структуры content слоя.
 *
 * Правило:
 * - UI обычно получает `GameContent` через application (`baseGameContent`), а не импортит отсюда напрямую,
 *   чтобы выбор pack’а оставался деталью композиции (application/app), а не UI.
 *
 * ## Инварианты / правила
 * - Этот файл экспортирует только “данные/контракты контента”.
 * - Здесь не должно появляться ничего из UI/React и не должно появляться доменной механики.
 */
export * from './gameContent';
export * from './definitions';
export * from './curses';
export * from './spells';
export * from './monsterAbilities';


