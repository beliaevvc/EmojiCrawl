# Reflection — Traveling Merchant: Непрозрачные токены (hotfix v1)

## Summary
Исправили визуальный баг: **токены торговца (артефакты и 🚪 “Уйти”) больше не просвечивают**, поэтому игрок **не видит карту/монету под ними**. Причина была в двух местах: полупрозрачные `bg-*/xx` классы у токенов и общая анимационная логика `opacity` для `isBlocked`.

## What Went Well
- Быстро локализовали источник: проблема сидела в одном компоненте рендера токенов (`CardComponent`), без необходимости трогать домен.
- Починили сразу **оба** канала “просвета”: фон и blocked‑opacity, поэтому баг не вернётся при “не хватает 💎”.

## Challenges Encountered
- `isBlocked` в UI используется не только для торговца (универсальный механизм), поэтому нельзя было просто убрать прозрачность глобально — нужно точечное исключение.

## Lessons Learned
- Для overlay‑токенов, которые кладутся поверх “живых” карт, **opacity как индикатор disabled — плохая идея**: она визуально раскрывает скрытое содержимое под токеном.
- Лучше разделять:
  - **disabled‑состояние**: `grayscale`/кольца/курсор/запрет drag,
  - **визуальную непрозрачность**: всегда 1 для оверлеев.

## Process Improvements
- В чеклист UI‑проверок для оверлеев добавить пункт: “**не просвечивает подложка** (особенно при disabled/blocked)”.

## Technical Improvements
- В `CardComponent` добавили понятное правило: **merchant‑токены не снижают opacity при blocked**, и используют непрозрачный фон.

## Next Steps
- (Опционально) Если захотим сильнее выражать “недоступно”, можно усилить disabled‑стейт без прозрачности: добавить `cursor-not-allowed`, `saturate-0`, более явный badge/иконку “🔒”.


