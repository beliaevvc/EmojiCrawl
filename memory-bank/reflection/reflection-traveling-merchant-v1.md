# Reflection: Traveling Merchant (Странствующий торговец) — v1

Дата: 2025-12-20

## Summary
Добавили внутриигровое событие “Странствующий торговец” как **overlay‑режим** внутри забега: на старте планируется (40% шанс), на случайном раунде открывается окно торговца, где игрок может **купить 1 предмет за 15💎** (DnD в рюкзак) или **уйти** (DnD на героя/в пустой слот). На время торговца большинство боевых действий блокируется, чтобы режим был “чистым” и предсказуемым.

**Verification:** пользователь подтвердил, что проверка ок (ручная), 2025-12-20.

## What went well
- **Variant B (overlay) оказался удачным**: минимально ломает текущий игровой флоу, не требует отдельных экранов/роутинга, проще контролировать блокировки действий.
- **DnD‑UX** для “покупки” и “уйти” хорошо ложится на существующую механику слотов/инвентаря.
- **Доменные защиты + UI‑валидация** (например, ограничения на продажу во время торговца) снижают риск “эксплойтов” и рассинхрона.

## Challenges
- **Границы между UI‑правилами и доменными инвариантами**: часть ограничений естественнее держать в домене, но UX‑ограничения хочется раннее/нагляднее показывать в UI.
- **Точки входа/выхода режима**: чтобы “возврат к обычному добору” был корректным, приходится аккуратно учитывать состояние стола и “последнюю карту прошлого стола”.
- **Сложность проверки**: режим событийный и завязан на шанс/раунд → важны детерминированные способы триггера (debug‑хук/кнопка) для ручной проверки.

## Lessons learned
- Для таких событий лучше заранее фиксировать **инварианты режима** (что можно/нельзя, что происходит при выходе) прямо в доменной логике и документации, иначе ограничения расползаются по UI‑хендлерам.
- Если событие зависит от случайности, полезно иметь **явный dev‑инструмент** для принудительного открытия режима (ускоряет проверку и баланс).

## Process improvements
- Перед реализацией новых “внутризабеговых режимов” полезно делать короткую таблицу:
  - **вход** (когда/при каких условиях)
  - **ограничения** (действия, DnD, продажи)
  - **выход** (что восстанавливаем/какие эффекты)
  - **инварианты** (что запрещено “на 100%”)
- Завести чеклист ручной проверки специально для событий: “открытие”, “покупка”, “уход”, “выход и продолжение добора”, “блокировки”.

## Technical improvements (follow-ups)
- Добавить **debug‑переключатель** (dev-only) для принудительного открытия торговца и/или фиксации `scheduledRound` (ускорит QA и баланс).
- Явно оформить контракт “merchant overlay” в механиках: вход/выход, ограничения, правила продажи, правила возврата к добору.

## Next steps
- Довести **партию 3** (реальные эффекты артефактов) и обновить `memory-bank/gameMechanics.md` по фактическим инвариантам.
- Когда артефакты станут полноценным пулом контента — описать их в `memory-bank/gameContent.md`.


