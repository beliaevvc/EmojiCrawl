# Traveling Merchant — Визуальная доработка (ТЗ)

## Назначение
Зафиксировать **ТЗ на визуальную/UX-доработку** механики **«Странствующий торговец»**: баннер‑табличка, подсказки/описания артефактов, единый “золотой” стиль артефактов (обводка + возможное переливание).

Документ должен стать “контрактом” для UI‑реализации и QA‑чеклиста.

## Источники истины
- **Дизайн‑система:** `memory-bank/uiDesign.md`
- **Механика торговца:** `memory-bank/travelingMerchant.md`
- **Код UI (после реализации):**
  - `src/components/game/board/GameBoard.tsx` (оверлей торговца)
  - `src/components/CardComponent.tsx` (рендер токена/кликабельность/эффекты)
  - `src/components/game/screens/GameSceneOverlays.tsx` или соседний слой (баннер над полем)

## 0) Принято (со слов пользователя)
- При активной механике торговца **над полем** должна висеть **красивая табличка/баннер в стиле UI**:
  - заголовок: **«Странствующий торговец»**
  - ниже: краткое описание **что происходит** и **что делать** (drag&drop купить/уйти)
  - баннер виден **всё время**, пока торговец активен.
- Баннер (уточнено):
  - **позиция**: по центру **над сеткой слотов**.
  - **размер**: 3–5 строк.
  - **формат подсказки**: **текст + иконки-инструкции**.
  - **“?”/“Подробнее”**: не нужно.
  - **закрываемость**: не закрывается/не сворачивается (всегда висит).
  - **пауза**: баннер остаётся “под паузой” (перекрывается паузой).
  - **проклятие “Тьма”**: тьма затемняет **всё**, включая баннер.
  - **анимация**: только pop-in при появлении (без постоянного пульса/анимации).
  - **важно по layout**: баннер **не сдвигает игровое поле вниз** — он висит отдельным overlay‑слоем **над** доской.
  - **точное позиционирование v1**: табличка крепится “над верхним рядом” через `bottom-full`,
    с небольшим отступом и дополнительным подъёмом примерно на **½ собственной высоты** (чтобы не “прилипала” к 4 верхним слотам).
- Эмоджи торговца в баннере: **🎩**
- **Артефакты торговца** должны быть **кликабельны**, чтобы открыть **описание как у заклинаний**.
- Описания артефактов (уточнено):
  - показываем **как у заклинаний** (тот же viewer/паттерн закрытия).
  - на телефоне: **тап = описание**, drag — **по удержанию**.
  - описание доступно **всегда** (и в магазине, и когда артефакт куплен/лежит в инвентаре).
  - в описании **не добавляем**: цену 15💎, явные строки “нельзя продать/продажа 0”, отдельную строку “как использовать”.
- Все артефакты торговца должны иметь **жёлтую обводку** и, возможно, **небольшое золотое переливание внутри**.
- “Золотой стиль” артефактов (уточнено):
  - **обводка**: стандартная толщина.
  - **лёгкое свечение**: допустимо.
  - **переливание**: мягкий градиентный перелив.
  - **цвет золота**: **amber-400** + лёгкий glow.
  - **скорость**: **3–4 секунды** на цикл перелива.
  - **интенсивность**: еле заметно (10–20%).
  - при “недостаточно 💎” shimmer **остается** (не выключаем).
  - 🚪 “Уйти” — **отдельный стиль**, не золотой.
- Важно: **токены торговца полностью непрозрачные** (не должны просвечивать и показывать карту/монету под ними), включая состояние “недостаточно 💎”.

## 1) Цели UX (зачем это делаем)
- Игрок сразу понимает, что это **особый режим**, и какие **2 действия** доступны (купить / уйти).
- Игрок может быстро прочитать **описания артефактов** без догадок.
- Визуально торговец и его товары ощущаются как **“редкое/ценное”** (золото), но не ломают общий dark fantasy стиль.

## 2) Баннер/табличка над полем (Merchant Banner)
### 2.1 Требования
- Рендерится только при `merchant.isActive === true`.
- Не мешает DnD: баннер должен быть **кликабельным только по интерактивным элементам**, но не блокировать перетаскивание по полю (возможны `pointer-events-none` на контейнере и `pointer-events-auto` на кнопках/линках).
- На мобильном и десктопе должен **влезать** без перекрытия ключевых слотов/аватара (адаптив).
- Баннер **не меняет** размеры/позицию поля (никаких `padding-top` у сетки; только absolute overlay).

### 2.2 Контент (черновик текста)
- Заголовок: **как у проклятий** (капс + tracking, в стиле UI-лейблов/баннеров)
- Тело (вариант): “Перетащи артефакт в свободный слот, чтобы купить (15 💎). Перетащи 🚪, чтобы уйти.”
- Доп. строка (если нужно): “Во время торговца бой и заклинания недоступны.”

### 2.3 Визуальный стиль (ориентир)
- В стиле “glassmorphism” из `uiDesign.md`:
  - `bg-stone-900/80`, `backdrop-blur-md`, `border border-stone-700`, `shadow-xl`
  - семантический акцент **Wealth/Amber** (например, тонкий `ring-amber-400/40` или градиент‑блик)
- Анимация: pop-in + легкий fade (framer-motion), без агрессивных движений.

## 3) Описание артефактов по клику (как у заклинаний)
### 3.1 Поведение
- Клик по артефакту торговца открывает стандартное окно/tooltip описания (как для spell‑карт).
- Описание должно включать:
  - название, иконку
  - 1–2 строки эффекта (коротко)
  - (опционально) ограничения/особенности, если они **уже входят** в базовое описание карты (без добавления новых “спец‑строк”)

### 3.2 Совместимость
- Не должно конфликтовать с DnD (клик не должен ломать drag; на тач‑устройствах нужен явный UX).
- Должно работать и в режиме торговца, и когда артефакт уже куплен (если он лежит в инвентаре).

## 4) Золотая обводка и “переливание” артефактов
### 4.1 Требования
- Для артефактов торговца: единая **жёлтая/золотая обводка** (вместо разноцветной по типам).
- Внутри токена возможно лёгкое **“gold shimmer”**:
  - небольшая анимированная подсветка/градиент, читаемая на тёмном фоне,
  - не должна мешать читабельности иконки/текста/бейджей.

### 4.2 Ограничения
- Должно быть безопасно по производительности (на поле одновременно 3 товара + 🚪).
- Уважать `prefers-reduced-motion` (если включено — shimmer выключается или становится статичным).

## 5) Состояния и фидбек
- Недостаточно 💎: товар визуально “недоступен” (existing rule), но **описание по клику** должно открываться.
- Нельзя купить из‑за web/нет слотов: баннер/описание может подсказать причину (решить в вопросах).

## 6) DoD / критерии готовности
- Баннер появляется при входе в режим торговца и остаётся до закрытия.
- Клик по любому артефакту открывает описание (мышь + тач).
- Все артефакты торговца имеют золотую обводку; shimmer соответствует согласованной интенсивности/анимации.
- Ничего не ломает DnD, паузу, существующие модалки и оверлеи (включая “Тьму”).

## 7) Открытые вопросы (нужно ответить перед реализацией)
> Отвечай пунктами (можно коротко). Чем точнее — тем меньше переделок в UI.

### A) Баннер/табличка
1) **Звук**: нужен ли короткий SFX при появлении торговца, или это вне этой задачи?

### B) Описания артефактов (клик/tooltip)
— Зафиксировано: описание “как у заклинаний”, доступно всегда, тач: tap=описание, hold-to-drag.
1) **Хочешь мини‑иконку‑схему** в описании (пиктограммы), или это лишнее? (по умолчанию: **не делаем**)

### C) Золотая обводка (единая) и shimmer
— Зафиксировано: amber‑400 + лёгкий glow; перелив 3–4 сек/цикл; при “не хватает 💎” shimmer **оставляем**.

### D) Мелкие UX‑детали
— Зафиксировано:
  - свободные слоты инвентаря **не подсвечиваем** (пока).
  - героя как drop‑цель для 🚪 **подсвечиваем**.
  - баннер **не меняет текст** при web/нет места.
  - заголовок — **как у проклятий**.

## Последнее обновление
- 2025-12-19 — зафиксированы ответы: 🎩 в баннере, amber‑400 + лёгкий glow, shimmer при disabled остаётся, скорость 3–4 сек/цикл.
- 2025-12-19 — финализировано позиционирование баннера: строго по центру над 4 верхними слотами, без сдвига поля, с дополнительным подъёмом ~½ высоты.


