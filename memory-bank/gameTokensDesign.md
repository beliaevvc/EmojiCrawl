# Дизайн-Система Игровых Сущностей (Design System)

Этот документ описывает визуальные стандарты отображения карт и элементов интерфейса Skazmor.
**Цель:** Обеспечить единообразие UI при добавлении новых механик.

## 1. Базовый Стиль Карты
Все карты в игре представляют собой **круглые токены** (Token-based design).

*   **Форма:** Круг (`rounded-full`).
*   **Размер:** Адаптивный (Base: `aspect-square`, Icon: `text-3xl` -> `text-5xl` на десктопе).
*   **Анимации:** `framer-motion` (Spring: stiffness 500, damping 30).
*   **Шрифты:**
    *   Основной текст/Иконки: Стандартный Sans.
    *   Заголовки: `Alegreya SC` (Serif).

## 2. Цветовое Кодирование (Color Coding)
Используется палитра Tailwind CSS для идентификации типа карты через границы (`border-2`) и фон.

| Тип Карты   | Border Color         | Background Color  | Пример     |
| :---        | :---                 | :---              | :---       |
| **Monster** | `border-rose-800`    | `bg-rose-950/40`  | Враг       |
| **Weapon**  | `border-stone-400`   | `bg-stone-800/80` | Меч        |
| **Shield**  | `border-stone-600`   | `bg-stone-800/80` | Щит        |
| **Potion**  | `border-emerald-700` | `bg-stone-800/80` | Зелье      |
| **Coin**    | `border-amber-500`   | `bg-stone-800/80` | Золото     |
| **Spell**   | `border-indigo-500`  | `bg-stone-800/80` | Заклинание |
| **Skull**   | `border-stone-600`   | `bg-stone-950/60` | Смерть     |

## 3. Элементы Интерфейса Карты (Badges & Indicators)

На базовом токене карты могут располагаться дополнительные индикаторы.

### A. Значение (Value Badge)
*   **Позиция:** Нижний правый угол (Bottom-Right).
*   **Стиль:** Маленький круг (`w-5 h-5` -> `w-7 h-7`).
*   **Цвета:**
    *   Обычный: `bg-stone-700`, `text-stone-200`.
    *   Монстр: `bg-rose-700`, `text-rose-100`.
    *   Debuffed (ослаблен): `bg-stone-900`, `text-rose-400`, `border-rose-500`.

### B. Способность Монстра (Ability Icon)
*   **Позиция:** Верхний центр (Top-Center), выступает за границу (`-top-3`).
*   **Стиль:** Круг `bg-stone-900`, `border-stone-600`.
*   **Контент:** Иконка способности (например, 🕸️ или ☠️).

### C. Ранг Монстра (Difficulty Dot)
*   **Позиция:** Верхний левый угол (Top-Left), внутри границы.
*   **Стиль:** Цветная точка (`w-3 h-3`).
*   **Кодировка:**
    *   🟢 Ordinary (`#10b981`)
    *   🟡 Tank (`#eab308`)
    *   🟠 Medium (`#f97316`)
    *   🟣 Mini-boss (`#a855f7`)
    *   🔴 Boss (`#e11d48`)

### D. Множитель Цены (Price Multiplier)
*   **Позиция:** Верхний левый угол (Top-Left), выступает за границу (`-left-2`).
*   **Стиль:** Желтый круг `bg-yellow-500`, черный текст.
*   **Пример:** `x2` (от эффекта "Скупщик").

### E. Индикатор Штрафа (Penalty)
*   **Позиция:** Снизу справа, под значением.
*   **Стиль:** Пульсирующий красный текст (`text-rose-500`, `animate-pulse`).
*   **Пример:** `-2` (урон монстра снижен).

## 4. Слоты (Slots)
Места, куда можно положить карту.

*   **Пустой слот:** Пунктирная граница (`border-dashed`), полупрозрачный фон (`bg-stone-800/30`).
*   **Placeholder:** Иконка на фоне (серый меч/щит), прозрачность 40%.
*   **Интерактивный (Drag Over):**
    *   Можно положить: `ring-2 ring-indigo-500`.
    *   Атака/Взаимодействие: `ring-2 ring-rose-500` (красная подсветка).
*   **Заблокирован:** `grayscale`, `opacity-50`, курсор `not-allowed`.

## 5. Анимации
*   **Shake:** При получении урона карта трясется (`translateX`).
*   **Hover:** Увеличение масштаба (`scale-105`).
*   **Drag:** Оригинал скрывается (`opacity-0`), перетаскивается "призрак".

## 6. Типографика (Global)
*   **Основной шрифт:** System Sans (`Segoe UI`, `Roboto`).
*   **Акцентный шрифт (Display):** `Alegreya SC` (для заголовков меню и важных надписей).
*   **Цвета текста:**
    *   Базовый: `stone-100` / `stone-200`.
    *   Muted: `stone-500`.
    *   Accent: `rose-500` (ошибки, атака), `emerald-500` (лечение), `amber-400` (золото).

