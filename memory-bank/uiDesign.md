# Дизайн-система и UI/UX (UI Design System)

> **Skazmor** использует мрачную, минималистичную эстетику "Dark Fantasy" с акцентом на тактильность карточного взаимодействия.

## 1. Цветовая Палитра (Color Palette)

Мы используем кастомизированную палитру Tailwind CSS, базирующуюся на цветах `stone` для интерфейса и семантических цветах для игровых событий.

### Основные цвета интерфейса
| Роль             | Tailwind Class             | Hex Code  | Описание                        |
| :---------------:| :------------------------: | :-------: | :-----------------------------: |
| **Background**   | `bg-stone-950` / `#141211` | `#0c0a09` | Глубокий черный фон приложения. |
| **Surface**      | `bg-stone-900`             | `#1c1917` | Основной фон панелей и окон.    |
| **Card Bg**      | `bg-stone-800`             | `#292524` | Фон карт и слотов.              |
| **Border**       | `border-stone-700`         | `#44403c` | Основной цвет границ элементов. |
| **Text Primary** | `text-stone-200`           | `#e7e5e4` | Основной текст.                 |
| **Text Muted**   | `text-stone-500`           | `#78716c` | Второстепенный текст, метки.    |

### Семантические цвета (Gameplay)
| Тип                 | Цвет (Base) | Классы (Bg/Text/Border)      | Применение                        |
| :------------------:| :----------:| :--------------------------: | :-------------------------------: |
| **Combat / Danger** | **Rose**    | `rose-500`, `rose-900`       | Монстры, Урон, Кнопки "New Game". |
| **Heal / Positive** | **Emerald** | `emerald-500`, `emerald-400` | Зелья, Лечение, Баффы.            |
| **Wealth**          | **Amber**   | `amber-400`, `amber-500`     | Монеты, Золото, Магазин.          |
| **Magic**           | **Indigo**  | `indigo-500`, `indigo-400`   | Заклинания, Магические эффекты.   |
| **Defense**         | **Blue**    | `blue-400`                   | Щиты, Броня (редко, чаще Stone).  |

## 2. Типографика (Typography)

### Шрифты
1.  **Display Font:** `Alegreya SC` (Serif, Small Caps).
    *   Используется для заголовков, логотипа, названий карт и важных кнопок.
    *   Класс: `font-display`.
2.  **Body Font:** `Inter` / `System UI` (Sans-serif).
    *   Используется для описаний, логов, интерфейсных текстов.
    *   Класс: `font-sans`.
3.  **Numbers:** `Monospace`.
    *   Используется для значений HP, урона и статистики.
    *   Класс: `font-mono`.

### Размеры текста
*   **Card Value:** `text-3xl` (mobile) / `text-5xl` (desktop).
*   **Headers:** `text-2xl` / `text-3xl`, часто с `uppercase tracking-widest`.
*   **Logs/Tooltips:** `text-[10px]` / `text-xs`.

## 3. Компоненты UI

### Карты (Cards)
Центральный элемент игры.
*   **Форма:** Круглая (`rounded-full`).
*   **Размеры:** Адаптивные, занимают 100% слота.
*   **Состояния:**
    *   *Default:* Border 2px, Shadow LG.
    *   *Hover:* Scale 1.05.
    *   *Dragging:* Opacity 0 (показывает слот под собой), копия следует за курсором.
    *   *Blocked:* Grayscale, Opacity 0.6.
*   **Значки (Badges):**
    *   *Value:* Маленький круг в правом нижнем углу.
    *   *Ability:* Круг сверху по центру.

#### Модификаторы значений (баффы/дебаффы на токенах)
Для некоторых эффектов (например, *Гниль*, проклятия *Отравление/Закалка/Жадность*) используется дополнительный маленький бейдж‑индикатор рядом со значением:
*   **Debuff:** красный (`rose`), показывает `-N`.
*   **Buff:** зелёный (`emerald`), показывает `+N`.
*   **Важно для монет (Жадность):** основной бейдж **не меняет номинал монеты**, а бонус отображается только как отдельный `+2`.

#### Проклятие “Тьма” (Darkness) — визуальный оверлей
Если активно проклятие **🌑 Тьма**, поверх контента отображается затемняющий слой с “дыркой” света вокруг курсора.
*   **Где используется:** `GameScreen` и `DeckbuilderScreen`.
*   **Требования к слоям (z-index):**
    * Оверлей **выше игрового поля/контента**, чтобы затемнять его.
    * Оверлей **ниже модалок** (`RulesModal`, `CursePicker`, и т.д.) и ниже информационных окон/HUD, чтобы интерфейс оставался читаемым.
*   **Интеракции:** `pointer-events-none` (не мешает drag&drop и кликам).
*   **Пасхалка:** во время активной “Тьмы” пасхалка **Lumos/Nox отключена**.

### Слоты (Slots)
Контейнеры для карт.
*   **Стиль:** Круглые, `aspect-square`.
*   **Пустой слот:** Пунктирная граница (`border-dashed`), полупрозрачный фон.
*   **Interaction Feedback:**
    *   *Can Drop:* `ring-2 ring-indigo-500`.
    *   *Interact (Attack):* `bg-rose-900/30 ring-rose-500`.

### Проклятье: слот/токен (Curse Slot)
Слот проклятия — “токен”-элемент (квадрат со скруглением `rounded-xl`), визуально похожий на жетон/монету по стилю.

*   **Расположение:** в игровом экране (`GameScreen`) **слева от кнопки “Сброс (-5HP)”** (в левом блоке управления).
*   **Состояния:**
    *   *Пусто и доступно:* пунктирная граница + “+”.
    *   *Активно:* отображается иконка проклятия как токен + tooltip (название/описание).
    *   *Пусто и заблокировано:* серый “✕” (если игрок уже сделал действие, но проклятие не выбрано).
*   **Подпись:** под токеном всегда есть маленькая подпись `Проклятье` (uppercase, в стиле UI-лейблов).
*   **UX-правило:** после первого действия игрока выбор проклятия блокируется.

### Баннер “Проклятие активировано”
Крупное уведомление над игровым полем, появляется при активации проклятия (и при старте кастомного забега с выбранным проклятием).

*   **Визуально:** крупная карточка/панель по центру, с иконкой проклятия, заголовком и описанием.
*   **Анимация:** pop-in + fade-out (framer-motion).

### Рубашка скрытых карт (Туман)
Для проклятия “Туман” используются скрытые карты со специальным отображением:

*   **Рубашка:** универсальный паттерн (без облака/иконки проклятия).
*   **Интеракции:** скрытые карты нельзя кликать/перетаскивать/выбирать целью.

### Окна и Панели (Glassmorphism)
Используется эффект матового стекла.
*   **Стили:**
    *   Background: `bg-stone-900/80` или `bg-stone-900/90`.
    *   Blur: `backdrop-blur-md` или `backdrop-blur-sm`.
    *   Border: `border border-stone-700` (тонкая обводка).
    *   Shadow: `shadow-xl`.

### Кнопки (SystemButton)
*   Прямоугольные со скруглением `rounded-xl`.
*   Содержат иконку (Lucide React) и текстовую метку.
*   **Hover Effect:** Легкое свечение градиентом (`from-indigo-900/20`), смещение вверх (`-translate-y-0.5`).

#### Расположение системного меню в бою
Кнопки **New Game / Пауза / Правила / Info** — это нижняя системная панель (`GameBottomBar`).
Она всегда должна быть **закреплена внизу экрана** (overlay-слой поверх сцены) и не “плавать” вверх по layout.

## 4. Анимации (Framer Motion)

Используем библиотеку `framer-motion` для плавности.

### Стандартные переходы
```javascript
transition={{ 
    type: "spring", 
    stiffness: 500, 
    damping: 30,
    mass: 1
}}
```

### Основные эффекты
1.  **Shake (Тряска):** При получении урона. `x: [0, -5, 5, -5, 5, 0]`.
2.  **Pop In (Появление):** `scale: [0.5, 1], opacity: [0, 1]`.
3.  **Floating Text (Всплывающий текст):**
    *   Появляется над объектом и улетает вверх, исчезая.
    *   Используется для отображения урона (`-5`), лечения (`+2`) и событий (`BLOCKED`).

## 5. Иконки
*   **Библиотека:** `lucide-react` (для UI элементов: Settings, Info, Reset).
*   **Эмодзи:** Используются внутри карт и для игровых событий (Wolf 🐺, Shield 🛡️, Coin 💎). Это осознанное стилистическое решение для упрощения восприятия.

## 6. Адаптивность (Responsive)
Интерфейс спроектирован по принципу **Mobile First**.
*   **Сетка:** `grid-cols-4` (одинакова на мобильных и десктопах, меняется размер ячеек).
*   **Размеры:** Все основные элементы имеют модификаторы `md:` для увеличения на десктопе (например, `text-xs md:text-sm`).
*   **Touch:** Все элементы поддерживают Touch Events (через `react-dnd` touch backend).

### Deckbuilder: скролл
Экран декбилдера (`DeckbuilderScreen`) использует layout:

*   **Header:** зафиксирован сверху.
*   **Footer (кнопки старта):** зафиксирован снизу.
*   **Контент:** находится в отдельной области с `overflow-y-auto`, чтобы новые секции (например, “Модификаторы”) не ломали вёрстку и были доступны на маленьких экранах.

---

## Приложение A — Спецификация игровых токенов и слотов (слито из `gameTokensDesign.md`)

> Этот раздел фиксирует низкоуровневые UI-стандарты “карта как токен” и “слот как контейнер”.
> Исторически они жили отдельно в `gameTokensDesign.md`, но теперь являются частью дизайн-системы.

### A1. Базовый стиль игровых токенов (карт)
- **Главный принцип**: карта на поле — это **токен**, а не прямоугольная карточка.
- **Форма**: по умолчанию **круглая** (`rounded-full`) — это относится к игровым картам (монстры/оружие/щит/зелье/монета/заклинание/череп).
- **Исключение**: некоторые UI-токены “не-карты” могут быть **`rounded-xl`** (например, `CurseSlot`), чтобы визуально отличаться от карт на столе.

### A2. Цветовое кодирование типа карты (границы/фон)
Используем типовые классы Tailwind (ориентир; точные оттенки могут варьироваться в UI, но “семантика” должна сохраняться).

| Тип карты   | Border (ориентир)       | Background (ориентир) | Примечание |
| :---        | :---                    | :---                  | :---      |
| Monster     | `border-rose-800`       | `bg-rose-950/40`      | Враг/опасность |
| Weapon      | `border-stone-400`      | `bg-stone-800/80`     | Физ. атака |
| Shield      | `border-stone-600`      | `bg-stone-800/80`     | Защита |
| Potion      | `border-emerald-700`    | `bg-stone-800/80`     | Лечение |
| Coin        | `border-amber-500`      | `bg-stone-800/80`     | Валюта/очки |
| Spell       | `border-indigo-500`     | `bg-stone-800/80`     | Магия |
| Skull       | `border-stone-600`      | `bg-stone-950/60`     | Мусор/смерть |

### A3. Индикаторы на токене (badges)
- **Value Badge**:
  - позиция: нижний правый угол
  - стиль: маленький круг (адаптивный)
  - задача: показать номинал (урон/прочность/лечение/монеты/HP монстра)
- **Ability Icon (для монстров)**:
  - позиция: верхний центр (может чуть “вылезать” наружу)
  - стиль: маленький круг с рамкой, иконка способности внутри
- **Difficulty Dot (лейбл/ранг монстра)**:
  - позиция: верхний левый угол (внутри токена)
  - задача: быстро различать “обычный/танк/минибосс/босс” по цвету
- **Price Multiplier**:
  - позиция: верхний левый угол (может “вылезать” наружу)
  - пример: `x2` для эффекта “Скупщик”

### A4. Состояния токена
- **Default**: border + shadow.
- **Hover**: лёгкий `scale` (примерно 1.05).
- **Dragging**: оригинал может скрываться (`opacity-0`), “призрак” следует за курсором.
- **Blocked/Disabled**: `grayscale` + `opacity` + `cursor-not-allowed`.

### A5. Слоты (Slots)
- **Форма**: как контейнер под токен (квадратная область `aspect-square` с круглой маской/обводкой по месту).
- **Пустой слот**: `border-dashed` + полупрозрачный фон.
- **Feedback DnD**:
  - *Can Drop*: подсветка кольцом (`ring-*`).
  - *Interact/Attack*: “опасная” подсветка (`rose`) по семантике.

---

## Последнее обновление
- 2025-12-19 — Слияние `gameTokensDesign.md` в `uiDesign.md`, фиксация обязательного формата документации (Блок 90).

