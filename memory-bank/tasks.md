# Task: «Проклятья» (Curse System)

## Статус
- [x] Planning
- [x] Implementation
- [x] Verification
- [x] Documentation

## Сложность: Level 3 (Intermediate)
Механика затрагивает **UI**, **Reducer/GameState**, **Deckbuilder**, и влияет на взаимодействия с картами на столе.

## Контекст / Текущее состояние кода (важно)
В проекте уже реализованы базовые части системы «Проклятий»:
- **Типы/State**: `CurseType`, `GameState.curse`, `DeckConfig.curse`, `Card.isHidden`.
- **Данные**: `src/data/curses.ts` (☁️ Туман, 🌕 Полнолуние).
- **Логика**:
  - Туман: скрывает 2 случайные карты на раздаче и раскрывает, когда на столе ≤2 карт.
  - Полнолуние: при смерти монстра лечит других монстров на +1 HP (не выше maxHP).
- **UI**:
  - Есть `CursePicker` и слот проклятия в `GameScreen`, но сейчас он расположен в верхней панели.

Задача — **довести UX до нужного поведения** (слот у игрового поля, подтверждение, блок взаимодействий со скрытыми картами, универсальная рубашка, баннер-уведомление, секция в декбилдере).

## Требования (по согласованию)

### 1) Размещение и поведение слота проклятия
- **Слот должен быть рядом с игровым полем** (рядом с сеткой/слотами, а не в верхней панели).
- В начале игры можно **стартовать без проклятия**.
- Пока игрок **не сделал действий**, проклятие можно выбрать позже.
- После применения проклятия оно **нельзя изменить**.
- Для применения нужен **шаг подтверждения** (“Вы хотите применить проклятие … ? Да/Нет”).

### 2) Визуал токена проклятия
- Проклятие должно отображаться как **монета/токен** (круглая фишка в стиле остальных “монет”).
- Доп. отображение в HUD/Stats **пока не требуется**.

### 3) Уведомление об активации
- **В лог — обязательно**.
- Дополнительно — **крупное сообщение/баннер над полем** (выше и заметнее, чем floating text).
- Баннер должен появляться и при старте **кастомного забега**, если проклятие уже задано.

### 4) Проклятие «Туман ☁️»
- Работает **каждый раунд**.
- Скрывает **2 случайные карты** (любых типов).
- Скрытые карты:
  - **нельзя трогать** (нельзя кликать/перетаскивать/применять на них действия).
  - имеют **универсальную рубашку** (без облака/иконки проклятия).

### 5) Проклятие «Полнолуние 🌕»
- При смерти монстра лечатся **все остальные монстры** (если есть недостающее HP).
- Работает для любой причины смерти (оружие/спелл/прочее), если смерть проходит через общий “kill” путь.

### 6) Декбилдер
- В декбилдере нужна **отдельная секция** под проклятие (в UX/верстке отделить от “Заклинаний” и базовых категорий).
- ⚠️ Нужна мини-уточнялка: фраза “и там не прокляти вроде как а заклинаеия” двусмысленна.
  - План предполагает: **сделать явный блок “Проклятия”**, а “Заклинания” оставить отдельной категорией как сейчас.
  - Если ты хотел другое (например, переименования/перемещения категорий) — уточним перед реализацией.

---

## План реализации (детально)

### A) GameState / правила “пока не сделал действий”
Цель: ввести однозначное правило, когда “плюс” доступен.
- **Добавить в `GameState` флаг** вроде `hasActed: boolean` (или `isRunLocked: boolean`) и сбрасывать его на `false` при `START_GAME`.
- **Переводить в `true`** при любых “игровых действиях”:
  - `TAKE_CARD_TO_HAND`
  - `INTERACT_WITH_MONSTER`
  - `USE_SPELL_ON_TARGET`
  - `SELL_ITEM`
  - `RESET_HAND`
- **Не считать действиями** чисто UI/технические:
  - `CHECK_ROUND_END`, `CLEAR_PEEK`, `CLEAR_SCOUT`, `TOGGLE_GOD_MODE`, `INIT_GAME`, `ACTIVATE_CURSE`
- UI-правило: если `state.curse === null` и `state.hasActed === false`, слот показывает “+” и разрешает выбор.

Файлы:
- `src/types/game.ts`
- `src/utils/gameReducer.ts`

### B) Подтверждение применения проклятия
Цель: выбор в пикере не применяет эффект мгновенно.
- В `GameScreen`:
  - При выборе в `CursePicker` сохранять `pendingCurse` в локальный state.
  - Показать `ConfirmationModal` (“Применить проклятие … ?”).
  - На Confirm → `dispatch({ type: 'ACTIVATE_CURSE', curse: pendingCurse })`.
  - На Cancel → просто закрыть подтверждение, `pendingCurse = null`.
- После подтверждения `state.curse` становится не-null → слот становится токеном и больше не кликается/не меняется.

Файлы:
- `src/components/GameScreen.tsx`
- `src/components/CursePicker.tsx` (опционально: только UI-текст/подсказки)
- `src/components/ConfirmationModal.tsx` (переиспользуем)

### C) Переместить слот проклятия к игровому полю (и оформить как “монету”)
Цель: сделать отдельный подключаемый компонент.
- Выделить компонент, например:
  - `src/components/CurseSlot.tsx` (или `components/game/CurseSlot.tsx`, если есть такая структура).
- Внутри:
  - Состояния: пусто (“+”), выбран токен, disabled.
  - Размер и стиль — как токен/монета (rounded-full, border, shadow), согласовать с `uiDesign.md` (token-like circle).
- В `GameScreen` разместить токен в **левом блоке управления**, **слева от кнопки “Сброс (-5HP)”** (в один ряд).

Файлы:
- `src/components/GameScreen.tsx`
- `src/components/CurseSlot.tsx` (новый)
- `memory-bank/uiDesign.md` (обновить после имплементации)

### D) «Туман»: запрет взаимодействия со скрытыми картами + универсальная рубашка
Цель: hidden = “не трогать”.
- На уровне UI:
  - `CardComponent`: если `card.isHidden`:
    - `canDrag = false` (даже если `isDraggable` true)
    - `onClick` игнорируется
    - value-badge/ability-badge не показывать
    - показывать “рубашку” (универсальный паттерн)
  - `EnemySlotDropZone` / handlers в `GameScreen`:
    - `handleDropOnEnemy` должен ранним return, если target card hidden.
    - любые “attack/target” операции по `card.isHidden` блокируются.
- “Рубашка универсальная”:
  - заменить текущий “☁️” на нейтральный узор (например, “✦/⛧/руны” с низкой opacity) без привязки к проклятию.

Файлы:
- `src/components/CardComponent.tsx`
- `src/components/GameScreen.tsx`
- (возможно) `src/components/Slot.tsx` / dropzones, если там есть логика кликов
- `memory-bank/uiDesign.md` (после имплементации)

### E) Баннер над полем при активации проклятия
Цель: крупное сообщение “Активировано проклятие …”.
- Вариант 1 (предпочтительный): отдельный UI слой `CurseActivationBanner` в `GameScreen`.
  - Триггер: `useEffect` на изменение `state.curse` (null → value).
  - Показ 1.5–2.5 секунды, затем fade-out (framer-motion).
  - Текст: “Проклятие активировано: {name}” + иконка.
- Вариант 2: расширить существующий `FloatingTextOverlay` (но он визуально “малый” по твоему запросу).

Файлы:
- `src/components/GameScreen.tsx`
- (опционально) `src/components/CurseActivationBanner.tsx` (новый)
- `memory-bank/uiDesign.md` (после имплементации)

### F) Декбилдер: отдельная секция проклятий
Цель: визуально и смыслово отделить “Проклятие” от “Заклинаний” и инвентарных категорий.
- Переразметить `DeckbuilderScreen`:
  - Секция 1: “Карта/инвентарь” (щит/оружие/зелья/кристаллы/заклинания/монстры).
  - Секция 2: “Модификаторы забега” (Проклятие).
  - В секции “Проклятие” показывать:
    - текущий выбор (иконка+название/“Нет”)
    - кнопку “Выбрать” (открывает `CursePicker`)
- При `START_GAME` кастомного забега: `deckConfig.curse` уже пробрасывается — оставить как есть, но обеспечить показ баннера на старте (см. пункт E).
 - Добавить **внутренний скролл** для декбилдера (фикс. header/footer + scrollable content), чтобы новые секции помещались на экране.

Файлы:
- `src/components/DeckbuilderScreen.tsx`
- `src/components/CursePicker.tsx` (без изменений или тексты/пояснения)
- `memory-bank/uiDesign.md` (после имплементации)

---

## Проверка (ручной чеклист)
- **Базовое**:
  - Новая игра без проклятия: слот с “+” доступен.
  - После любого действия: “+” исчезает/становится disabled (если проклятие не выбрано) и выбрать уже нельзя.
  - После подтверждения проклятия: изменить уже нельзя.
- **Туман**:
  - Каждый раунд скрывает 2 карты.
  - Скрытые карты нельзя перетащить/кликнуть/атаковать/продать.
  - При уменьшении карт на столе до 2 — карты раскрываются и снова доступны.
- **Полнолуние**:
  - Убить монстра любым способом → остальные монстры лечатся на +1, но не выше maxHP.
- **Декбилдер**:
  - Выбор проклятия сохраняется в конфиге.
  - Кастомный старт с проклятием → проклятие активно, лог есть, баннер показан.

**Статус проверки:** проверено пользователем, 2025-12-18 — “вроде всё работает”.

## Документация (обновить при реализации)
- `memory-bank/gameMechanics.md`:
  - описание “Проклятий”
  - правило “можно выбрать только до первого действия”
  - эффекты Тумана/Полнолуния
- `memory-bank/gameContent.md`:
  - каталог проклятий (иконки, описания)
- `memory-bank/uiDesign.md`:
  - описание слота проклятия у поля, внешний вид токена, баннер-уведомление, рубашка скрытых карт

---

# Task: «Проклятья» — расширение списка (🥦 Отравление, 🛠️ Закалка, 💰 Жадность)

## Статус
- [x] Planning
- [x] Implementation
- [x] Verification
- [x] Documentation

## Сложность: Level 3 (Intermediate)
Затрагивает **баланс-логику** в `gameReducer.ts` + **визуальные модификаторы** значений на токенах (зелья/оружие/кристаллы).

## Требования (как понял сейчас)
- **🥦 Отравление**: все зелья лечат на 1 HP меньше. Нужен визуальный индикатор “было/стало” на токене зелья.
- **🛠️ Закалка**: все оружия наносят на 1 урон больше. Нужен визуальный индикатор “было/стало” на токене оружия.
- **💰 Жадность**: все кристаллы дают +2 💎. Нужен визуальный индикатор “было/стало” на токене кристалла.

## Актуальные места в коде (где внедрять эффект)
- **Зелья (лечение)**: `src/utils/gameReducer.ts` → `TAKE_CARD_TO_HAND` → ветка `card.type === 'potion'` (сейчас уже есть модификатор “Гниль: -2”).
- **Оружие (урон)**: `src/utils/gameReducer.ts` → `handleWeaponAttack()` (`damage = weapon.value`).
- **Кристаллы (получение монет)**: `src/utils/gameReducer.ts` → `TAKE_CARD_TO_HAND` → ветка `card.type === 'coin'` (`player.coins += card.value`), плюс там есть спец-кейс **Подношение** (монеты лечат монстров).
- **Визуальный индикатор**: `src/components/CardComponent.tsx` уже умеет показывать “модифицированное значение” через проп `penalty` (используется для “Гниль -2” на зельях в `GameScreen`).

## План реализации (черновик)
### A) Данные/типы
- Расширить `CurseType` в `src/types/game.ts`:
  - добавить новые id (например: `poison`, `tempering`, `greed` — уточним нейминг).
- Расширить список `CURSES` в `src/data/curses.ts` (name/description/icon/color).

### B) Логика эффектов (gameReducer)
- **Отравление**:
  - уменьшать эффект лечения зелья на 1 (но не ниже 0), аналогично как сейчас сделано для “Гнили”.
  - обновить лог: показывать, что сработало “ОТРАВЛЕНИЕ: -1”.
- **Закалка**:
  - добавить +1 к урону оружия в `handleWeaponAttack`.
  - проверить связанные эффекты: `miss` (-2) и прочие модификаторы должны корректно стакиваться.
  - проверить `updateMirrorMonsters` (там считается max урон оружия) — нужно ли учитывать +1 от Закалки.
- **Жадность**:
  - добавить +2 к получаемым 💎 при поднятии кристалла.
  - уточнить, влияет ли +2 на “Подношение” (когда монеты лечат монстров) — см. вопросы.

### C) Визуал “было/стало” на токенах
- В `src/components/GameScreen.tsx` вычислять модификатор для `CardComponent`/`Slot`:
  - для `potion`: -1 при Отравлении (плюс уже существующая Гниль -2) → передавать суммой.
  - для `weapon`: +1 при Закалке.
  - для `coin`: +2 при Жадности.
- В `src/components/CardComponent.tsx` (минимально):
  - улучшить отображение модификатора: для положительных значений показывать `+N` и зелёный стиль (сейчас любой non-zero подсвечивается красным как дебафф).
  - оставить совместимость с текущим использованием “Гниль -2”.

### D) Проверка
- Зелья: значение на бейдже и фактическое лечение совпадают (с учётом clamp/overheal).
- Оружие: значение на бейдже и фактический урон совпадают, стакается с `miss`.
- Кристаллы: значение на бейдже и фактическое начисление 💎 совпадают, проверить кейс “Подношение”.

## Уточняющие вопросы (нужно решить перед кодом)
1) **Жадность и “Подношение”**: если на столе есть монстры с абилкой `offering`, монеты идут в лечение монстров. В этом кейсе **+2 должно добавляться** (т.е. монета “сильнее лечит”), или Жадность влияет **только на 💎 игрока**?
2) **Закалка и “Зеркало”** (`mirror`): этот монстр берёт силу равную максимальному урону оружия. При Закалке он должен брать **(value+1)** или **только базовый value**?
3) **Отравление и прочие источники лечения**: Отравление режет **только зелья** (карты `type === 'potion'`) или ещё и лечение от заклинаний (`leech`) / абилок (например `blessing`)?
4) **Порядок стакинга модификаторов**:
   - для зелья сейчас есть “Гниль -2”. При Отравлении это будет **-3 суммарно**? (я бы сделал именно так)
   - для оружия: сначала `miss` (-2), потом Закалка (+1) или наоборот? (по факту это просто сумма, но важно для объяснения/логов)
5) **Как именно показываем “было/стало” визуально?**
   - вариант A: основной бейдж показывает **итог**, рядом маленький бейдж `(+1/-1/+2)`.
   - вариант B: показывать **оба числа** (например “3 → 4”) прямо на токене.
   Сейчас в `CardComponent` уже есть вариант A для дебаффов — подтюним под баффы.

---

# Task: «Проклятье — Тьма 🌑» (front-only, как Lumos)

## Статус
- [x] Planning
- [x] Implementation
- [x] Verification
- [x] Documentation

## Сложность: Level 2 (Simple Enhancement)
UI‑фича: визуальный слой (оверлей) + добавление нового проклятия в список выбора. Без изменения боевой логики/редьюсера, кроме “активно проклятие” в UI.

## Краткое описание
Если активировано проклятие **«Тьма»**, экран “погружается во тьму”, а вокруг **курсора мыши** есть круг света (как текущая пасхалка **Lumos**).

## Что уже есть в коде (можно переиспользовать)
- `src/components/FlashlightOverlay.tsx`: реализован “темный слой + круг света” через `maskImage/WebkitMaskImage` и трекинг мыши.
- `src/App.tsx`: уже подключает `FlashlightOverlay` глобально.

## Уточнения (нужно подтвердить перед реализацией)
1) **Где работает “Тьма”**: **в игре (`GameScreen`) и в декбилдере**.
2) **Источник света**: **вокруг мышки**.
3) **Радиус/мягкость**: **как у Lumos** (текущие параметры `FlashlightOverlay`).
4) **Совместимость с пасхалкой Lumos/Nox**: при активной “Тьме” пасхалка **отключается/игнорируется**.
5) **UI‑слои (z-index)**: оверлей **ниже модалок** и **не мешает Info/HUD**, `pointer-events-none`.

## План реализации

### A) Контент/тип проклятия
- Добавить новый `CurseType` id: `darkness` (или `dark` — выбрать одно имя и везде использовать).
- Добавить запись в `src/data/curses.ts`:
  - name: `Тьма`
  - icon: `🌑` (или другой — уточним)
  - description: “Всё темнеет, вокруг курсора — круг света.”
  - color: `text-stone-200`/`text-indigo-200` (визуально обсудим)

Файлы:
- `src/types/game.ts`
- `src/data/curses.ts`

### B) Реализация оверлея (переиспользовать Lumos)
Есть 2 рабочих варианта — выберу самый чистый по UX/поддержке:

**Вариант 1 (предпочтительный): выделить переиспользуемый компонент**
- Создать компонент, например `src/components/MaskedFlashlightOverlay.tsx`:
  - проп `enabled: boolean`
  - пропы для параметров: `radiusPx`, `softEdgePercent`, `zIndexClass`
  - внутри: трек мыши + `maskImage` как в `FlashlightOverlay`
- `FlashlightOverlay` переписать как тонкую обертку над `MaskedFlashlightOverlay` (enabled от пасхалки).
- В `GameScreen` при `state.curse === 'darkness'` рендерить `MaskedFlashlightOverlay enabled` **внутри GameScreen**, с `z` ниже модалок/инфо‑HUD.
- В `DeckbuilderScreen` при выборе проклятия `darkness` в конфиге — рендерить такой же оверлей (тот же z‑порядок).
- Добавить “лок” для пасхалки: пока активна Тьма, `FlashlightOverlay` не включается от `lumos/svet` и не выключается от `nox` (игнор).

**Вариант 2 (быстрый, но менее аккуратный):**
- Не трогать `FlashlightOverlay`, а просто добавить второй почти такой же оверлей в `GameScreen`.
- Минус: дублирование логики/стилей.

Файлы:
- `src/components/FlashlightOverlay.tsx` (скорее всего рефактор)
- `src/components/GameScreen.tsx`
- (возможно новый) `src/components/MaskedFlashlightOverlay.tsx`

### C) Правила отображения и слои
- Оверлей Тьмы должен быть `pointer-events-none`.
- Убедиться, что модалки (`CursePicker`, `RulesModal`, `GameStatsOverlay`) остаются **выше** оверлея.
- Убедиться, что **Info/HUD** остаётся читаемым/видимым (оверлей не должен перекрывать модальные окна и HUD-панели).
- Убедиться, что “Тьма” не ломает drag&drop (она не должна перехватывать события).

### D) Проверка (ручной чеклист)
- Выбрать проклятие **Тьма** → экран темнеет, “дырка” следует за курсором.
- Перетаскивание карт/клики работают как раньше (оверлей не мешает).
- Модалки поверх (CursePicker/Rules/статы) читаемы и не затемняются.
- Переключение на другое состояние/новую игру корректно убирает эффект.

## Документация (после реализации)
- `memory-bank/gameMechanics.md`: добавить описание проклятия “Тьма” (как визуальный модификатор).
- `memory-bank/gameContent.md`: добавить строку в таблицу проклятий.
- `memory-bank/uiDesign.md`: коротко описать слой “Тьма” и требования по z-index/UX.


